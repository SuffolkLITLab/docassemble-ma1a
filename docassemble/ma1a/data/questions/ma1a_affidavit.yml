---
comment: |
  Affidavit of Irretrievable Breakdown for MA 1A Divorce
  
  This form requires one spouse to attest that the marriage has broken down
  irretrievably and that there is no chance of reconciliation.
  
  Most data is already collected in shared_data and cjd101a modules.
---
code: |
  log("[affidavit] Starting Affidavit of Irretrievable Breakdown data collection")
  
  # Reuse data already collected:
  # - users[0], other_parties[0] (from shared_data)
  # - marriage_date, last_lived_together_date (from shared_data)
  # - breakdown_date (from cjd101a)
  # - trial_court (from shared_data)
  
  # Determine which party is signing the affidavit (typically Party A/users[0])
  affidavit_signer = users[0]
  log(f"[affidavit] Affidavit signer: {affidavit_signer.name.full()}")
  
  # Collect any affidavit-specific information if needed
  # (Most affidavits just require confirmation, no new data)
  
  # Mark affidavit as complete
  affidavit_complete = True
  ma1a_case.affidavit_complete = True
  log("[affidavit] Affidavit of Irretrievable Breakdown data collection COMPLETE ✓")

